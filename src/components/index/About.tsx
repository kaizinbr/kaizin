"use client";

import { motion } from "framer-motion";

export default function About() {
    return (
        // <motion.div whileHover={{ scale: 1.2 }} whileTap={{ scale: 0.8 }} />
        <div
            className={`
            card
            h-96 w-full overflow-hidden
        `}
        >
            
            
        </div>
    );
}
